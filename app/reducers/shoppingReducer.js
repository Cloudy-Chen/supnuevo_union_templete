/**
 * shoppingReducer.js
 */
import * as actions from "../actions/action-types";

export default function shoppingReducer(state, action = {}) {
    switch (action.type) {
        default:
            return state
    }
}

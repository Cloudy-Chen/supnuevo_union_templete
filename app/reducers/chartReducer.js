/**
 * chartReducer.js
 */
import * as actions from "../actions/action-types";

export default function chartReducer(state, action = {}) {
    switch (action.type) {
        default:
            return state
    }
}
